# SoukMar Android — Claude Working Guide

## Projekt
Native Android-App (Kotlin, Jetpack Compose) für SoukMar, das marokkanische Kleinanzeigen-Portal.
Spricht direkt mit dem bestehenden Backend — **kein separates Android-Backend**, gleiche API wie die Web-App.

**Pfad:** `C:\AIProjekte\soukmar-android`
**GitHub:** github.com/azizennajih/soukmar-android
**Backend:** `C:\AIProjekte\soukmar-backend` (Port 3000) — siehe dessen eigenes `CLAUDE.md`
**Web-App (Referenz für Verhalten/Copy):** `C:\AIProjekte\soukmar`

Die Android-App wird **phasenweise** mit vollem Funktionsumfang aufgebaut (Nutzerentscheidung: "Android zuerst", "voller Funktionsumfang" statt MVP). Bei jeder neuen Funktion zuerst die entsprechende Web-Implementierung lesen (Component/Service/Route) und exakt spiegeln — Feldnamen, Endpunkte, Validierungsregeln, Copy-Ton.

---

## Stack
- Kotlin + Jetpack Compose (Material 3)
- Hilt (Dependency Injection)
- Retrofit + OkHttp + kotlinx.serialization (REST)
- Socket.IO Client (Chat, Echtzeit — Dependency vorhanden, noch nicht verdrahtet)
- Coil (Bilder)
- DataStore (Session/Token, entspricht `localStorage` im Web-Frontend)
- Navigation Compose
- osmdroid (Karten — OpenStreetMap-Kacheln, spiegelt Webs Leaflet/OSM; kein API-Key nötig, im Gegensatz zu Google Maps)

**Package:** `com.soukmar.app` · `minSdk 26` · `compileSdk`/`targetSdk 34`

---

## Build-Toolchain (dieser Rechner)

Auf diesem Windows-Rechner mussten JDK 17, Android SDK und Gradle-Wrapper manuell installiert werden (kein Android Studio vorinstalliert). Bei einem frischen Checkout:

```
sdk.dir=C:/Android/sdk
```
in `local.properties` eintragen (gitignored, existiert nicht nach `git clone`).

| Was | Pfad |
|-----|------|
| JDK 17 | `C:\Android\jdk17\jdk-17.0.12+7` |
| Android SDK | `C:\Android\sdk` |
| Gradle (Wrapper lädt automatisch 8.7) | — |

Build: `JAVA_HOME=C:\Android\jdk17\jdk-17.0.12+7` setzen, dann `./gradlew assembleDebug`.

Debug-Build zeigt auf `http://10.0.2.2:3000/api/` (Emulator-Alias für den Host-PC). Bei einem **echten Gerät** statt Emulator die LAN-IP des Rechners in `app/build.gradle.kts` (`API_BASE_URL`) eintragen.

**Bekanntes Problem:** Der Android-Emulator läuft innerhalb der Claude-Code-Sandbox (verschachtelte Virtualisierung, Software-Rendering) teils extrem langsam — bis hin zu System-weiten ANRs (auch bei fremden System-Apps, nicht nur SoukMar). Das ist kein App-Fehler. Mildernd: niedrigere AVD-Auflösung (z.B. 720×1280 statt 1080×2340) und `hw.cpu.ncore=1` reduzieren die Last spürbar. Texteingabe per `adb shell input text` ist auf dem langsamen Emulator unzuverlässig (Zeichen gehen verloren) — zeichenweise mit kleinen Pausen (`sleep 0.3-0.5`) senden statt am Stück.

**Firebase/FCM-Setup (Phase 11, noch offen):** `app/google-services.json` ist ein **Platzhalter mit Fake-Werten** (nur strukturell valide, damit `com.google.gms.google-services` den Build nicht bricht) — für echten Push muss ein reales Firebase-Projekt angelegt und die echte `google-services.json` hier eingespielt werden, **plus** im `soukmar-backend`-Repo die Env-Var `FIREBASE_SERVICE_ACCOUNT` (JSON des Service-Account-Keys als String) gesetzt werden. Ohne das no-opt der Push-Versand serverseitig lautlos (kein Fehler, kein Crash) — die In-App-Benachrichtigungsliste funktioniert unabhängig davon bereits vollständig.

---

## Design System — IMMER mit `soukmar/src/styles.scss` synchron halten

`ui/theme/Color.kt` spiegelt die CSS-Variablen der Web-App 1:1:

| Compose-Konstante | Wert | Verwendung |
|--------------------|------|------------|
| `Primary` | `#D93D4A` | Buttons, CTAs, Logo, Hover |
| `PrimaryDark` | `#B8313C` | Hover-Zustand |
| `PrimaryLight` | `#FEF1F2` | Hintergrund für Primär-Highlights |
| `Gold` | `#C9941A` | Premium-Badges |
| `GoldLight` | `#FEF6E4` | Hintergrund für Gold-Highlights |
| `Secondary` | `#1E3A5F` | sekundäre Elemente |
| `TextPrimary` | `#111827` | Haupttext |
| `TextMuted` | `#6B7280` | Sekundärtext |
| `BorderColor` | `#E5E9EE` | Rahmen |
| `BgColor` | `#F8F7F5` | Seitenhintergrund |
| `ErrorColor` | `#DC2626` | Fehler |
| `SuccessColor` | `#16A34A` | Erfolg |

Kategorie-Badge-Farben (bg/fg-Paare) in `ui/model/CatalogModels.kt` — spiegeln `.cat-*`-Klassen aus `soukmar/src/styles.scss`.

**Keine hardcodierten Farben** außerhalb von `Color.kt` — immer die benannten Konstanten importieren.

---

## i18n — seit Phase 13 vorhanden (6 Sprachen wie Web)

Neue UI-Texte **immer** über `t("namespace.key")` (aus `com.soukmar.app.ui.i18n`) schreiben, nie mehr hartcodiertes Französisch. Die Übersetzungsdateien sind `app/src/main/assets/i18n/{fr,en,ar,de,es,it}.json` — **1:1-Kopien der Web-Dateien** (`soukmar/src/assets/i18n/`), nicht manuell pflegen. Ein neuer Key muss zuerst im Web-Repo existieren (oder dort mit ergänzt werden) und dann per Datei-Kopie hier landen — nicht eigenständig in der Android-JSON erfinden, sonst laufen beide Apps auseinander. Für Backend-getriebene Katalog-Codes (Kategorie/Subkategorie/EAV-Attribut/-Option) `tCatalog("cats.$code", code)` etc. verwenden (Fallback auf `humanizeCode()`). Details, Architektur und bekannte Lücken siehe Phase-13-Eintrag im Phasenplan unten.

---

## Architektur-Konventionen

- **State:** `HiltViewModel` mit `var x by mutableStateOf(...)` (Compose State), **kein** `StateFlow`/`LiveData` — durchgängiges Muster in diesem Projekt.
- **API-Ergebnisse:** `sealed class ApiResult<out T> { Success<T>(data); Error(message, unverified=false) }` in `data/repository/ApiResult.kt`-Kontext (definiert in `AuthRepository.kt`). Jedes Repository hat eine eigene private `parseError()`-Kopie (bewusst dupliziert, kein gemeinsames Basis-Repository — Konvention beibehalten).
- **DTOs:** `@Serializable data class` in `data/remote/dto/`, benannt nach Domäne (`AuthDto.kt`, `ListingDto.kt`, `CatalogDto.kt`, `ReviewDto.kt`, `ReportDto.kt`, `ChatDto.kt`). Felder nullable mit Default, wenn die Backend-Route sie weglassen kann.
- **Repositories:** `@Singleton`, injizieren `ApiService` + `Json` (für Fehler-Parsing). Ein Repository pro Domäne (Auth, Listing, Catalog, Review, Report, Chat).
- **Compose Screens:** Datei-Header `@file:OptIn(ExperimentalMaterial3Api::class)` (und `ExperimentalFoundationApi::class` bei Pager-Nutzung) statt einzelner `@OptIn` pro Funktion.
- **Navigation:** `Routes.kt` als zentrales Objekt mit Konstanten + Helper-Funktionen für parametrisierte Routen (`Routes.listingDetail(id)` statt String-Interpolation im Aufrufer).
- **Farben/Kategorien:** `ui/model/CatalogModels.kt` — statische Kopie von `CATEGORIES`/`CONDITION_CATEGORIES`/`HIGHLIGHT_ATTR_CODES` aus `listing.model.ts`. Bei Änderungen am Web-Katalog **beide Seiten** aktualisieren.
- **"Tap to open a dropdown" auf einem `readOnly` `OutlinedTextField`:** ein `.clickable{}` direkt auf dem TextField feuert unzuverlässig nicht — das TextField konsumiert den Tap selbst zuerst für seinen eigenen Fokus/Cursor. Stattdessen einen transparenten Overlay-`Box(Modifier.matchParentSize().clickable{...})` als Geschwister-Element *über* dem TextField platzieren (siehe `CityDropdown`/`AttributeField` SELECT-Case in `DeposerAnnonceScreen.kt`).

---

## Dynamisches Attribut-System (EAV) — wie im Web

Kategorien haben Unterkategorien (`Subcategory`), jede mit eigenen `AttributeDefinition`-Feldern (Text/Zahl/Auswahl/Ja-Nein, Pflicht-/Filter-Flag). Werte pro Anzeige in `ListingAttributeValue`. Katalog-Endpunkte unter `/api/catalog/*` (`CatalogRepository`). Suchfilter nutzen `attr_<CODE>` (Auswahl/Bool) bzw. `attr_<CODE>_min`/`_max` (Zahl-Bereich) — siehe `ListingsViewModel.buildParams()`.

---

## Phasenplan (voller Funktionsumfang, Android zuerst)

Reihenfolge und Stand:

1. ✅ Auth (Login/Register/Passwort vergessen+zurücksetzen)
2. ✅ Home + Anzeigen durchsuchen/suchen mit dynamischen EAV-Filtern
3. ✅ Anzeigendetail (Galerie, Attribute, Preis-Indikator, Favoriten, Bewertung, Melden, Chat-Einstieg)
4. ✅ Anzeige aufgeben (Assistent mit dynamischen Attributen, Foto-Upload, Premium/Telefon-Toggle) — `ui/screens/deposerannonce/`. ViewModel unterstützt bereits Edit-Modus (optionaler `id`-Routenparameter, `ListingUpsertRequest` für POST **und** PUT), aber im NavGraph ist aktuell nur der Create-Einstieg (FAB auf Home) verdrahtet — der Edit-Einstieg kommt mit Phase 6 (Mes Annonces → "Bearbeiten"-Button navigiert zu `Routes.deposerAnnonce(listing.id)`). Foto-Picker nutzt `ActivityResultContracts.GetMultipleContents()` (kein Photo-Picker-Play-Services-Zwang). Reihenfolge/Hauptbild wird per Tap-to-promote gelöst (kein Drag&Drop wie im Web).
5. ✅ Chat (Socket.IO Echtzeit, Angebote, Schnellantworten, Melden) — `ui/screens/chat/` (Liste + Konversation), `data/remote/ChatSocketManager.kt`. Gleiches Event-Protokoll wie `soukmar-backend/src/socket.ts` (`send_message`, `send_offer`, `respond_offer`, `cancel_offer`, `cancel_reservation`, `typing` → `new_message`, `offer_updated`, `user_typing`, `listing_status_changed`). Es gibt **kein** eigenes REST-Endpoint zum Senden — Nachrichten/Angebote laufen ausschließlich über den Socket. Einstieg über Chat-Icon in `HomeScreen`-TopAppBar sowie über "Contacter le vendeur" in `ListingDetailScreen` (navigiert jetzt direkt in die Konversation statt der alten Platzhalter-Snackbar).
6. ✅ Meine Anzeigen (Bump, 14-Tage-Statistik) — `ui/screens/mesannonces/`. Liste eigener Anzeigen mit Status-Badge, Aktionen (Voir/Réserver-Toggle/Modifier/Bump/Statistiken/Supprimer). "Modifier" nutzt den bereits in Phase 4 vorbereiteten Edit-Modus von `DeposerAnnonceScreen` (`Routes.deposerAnnonce(id)`) — der Nav-Eintrag dafür fehlte bisher und ist jetzt verdrahtet. Einstieg über Icon in `HomeScreen`-TopAppBar.
7. ✅ Favoriten-Übersicht — `ui/screens/favoris/`. Grid (2 Spalten) aus `ListingCard` mit Herz-Icon-Overlay zum direkten Entfernen (optimistisches Update), nutzt bereits vorhandenes `GET /api/favorites`. Einstieg über Icon in `HomeScreen`-TopAppBar.
8. ✅ Profil (bearbeiten + Passwort ändern) — `ui/screens/profil/`. Avatar-Upload (nutzt `UploadRepository` aus Phase 4), Name/Telefon/Ville bearbeiten, Passwort ändern. Backend-Route `PUT /auth/profile` aktualisiert nur die im Body vorhandenen Felder — deshalb zwei getrennte Request-DTOs (`ProfileUpdateRequest` ohne `image`-Feld, `ProfileImageUpdateRequest` nur mit `image`), sonst würde ein Profil-Save das Avatarbild mit explizitem `null` löschen. `HomeScreen`-TopAppBar wurde dabei aufgeräumt: Profil+Logout stecken jetzt hinter einem ⋮-Overflow-Menü statt als eigene Icons.
9. ✅ Verkäuferprofil (Bewertungen, Antwortzeit) — `ui/screens/sellerprofile/`, Route `vendeur/{id}`. Lädt drei Requests parallel: `GET users/{id}/profile` (Name/Ville/Foto/Mitglied-seit/Ø-Bewertung/Anzahl/aktive-Annoncen/Ø-Antwortzeit), `GET users/{id}/listings` (aktive Annoncen des Verkäufers, wiederverwendet `ListingCard` in paarweisen Zeilen statt verschachteltem LazyVerticalGrid — vermeidet unbounded-height-Konflikt mit der äußeren LazyColumn), `GET reviews/user/{userId}` (erhaltene Bewertungen inkl. Reviewer+Annonce-Referenz, neues `ReviewWithDetailsDto`/`ReviewsForUserResponse`, neues `UserRepository`). Antwortzeit-Text dreistufig wie Web (`< 1h` Minuten, `< 24h` Stunden, sonst Tage). Sternebewertung als reine `★`-Glyphen (gefüllt/grau via `Gold`/`BorderColor`), Rundung wie Web (`roundToInt()`). Einstieg: neuer "Voir ses annonces"-Button unter der `SellerCard` in `ListingDetailScreen` (vorher rein statische Karte ohne Aktion) — mirror des Webs, wo nicht der Name selbst, sondern ein separater Button verlinkt. Die bereits aus einer früheren Phase vorhandene Bewertungsabgabe (`ReviewCard` in `ListingDetailScreen`, `POST /reviews`) war schon fertig verdrahtet und wurde nur end-to-end mitgetestet (Bewertung abgeben → erscheint sofort korrekt im Verkäuferprofil des Empfängers, inkl. Ø-Neuberechnung).
10. ✅ Gespeicherte Suchen — `ui/screens/savedsearches/`, Route `recherches_sauvegardees`, Einstieg im ⋮-Overflow-Menü von `HomeScreen` (oberhalb Profil). CRUD gegen `/api/saved-searches` (`GET`/`POST`/`DELETE`, neues `SavedSearchDto`/`SavedSearchRepository`) — spiegelt typisierte Spalten des Backends (`category`, `subcategoryId`, `q`, `city`, `minPrice`, `maxPrice`, `condition`, `attrs`), nicht ein serialisiertes Filter-Blob. `attrs` speichert wie im Web nur SELECT/BOOLEAN-EAV-Auswahlen (`Map<String, List<String>>`), NUMBER-Ranges werden bewusst nicht persistiert (Web verhält sich identisch). **Kein City-Filter**: `ListingsViewModel` hat aktuell überhaupt kein Stadt-Filterfeld (im Gegensatz zum Web) — `city` wird beim Speichern daher nie gesetzt; bewusste Entscheidung, um keine komplette City-Filter-UI als Nebenprodukt dieser Phase einzuführen. "Enregistrer cette recherche" sitzt in `ListingsScreen` direkt unter der Kategorie-Chip-Zeile (immer sichtbar wenn `isLoggedIn`, analog zur Web-Platzierung unterhalb der Sortierung), mit Inline-Formular (Name-Feld, Annuler/Enregistrer) und 3s-Erfolgsmeldung. Eine gespeicherte Suche erneut öffnen navigiert über einen neuen optionalen Query-Param `Routes.listings(savedSearchId=...)` (Route jetzt `listings?category={category}&savedSearchId={savedSearchId}`) — `ListingsViewModel.applySavedSearchById()` lädt die Liste (kein GET-by-id-Endpoint vorhanden), findet den Eintrag und stellt Kategorie (inkl. Nachladen der Sous-catégorie/Attribute via `getCategoryFull`), Sous-catégorie, État, Preisspanne, Stichwort und EAV-Auswahlen wieder her, bevor neu gesucht wird. End-to-end mit echtem Backend getestet: Speichern mit Kategorie+Preis+Marke-Attribut (Backend-Response 1:1 verifiziert), Wiederherstellen → korrekt gefilterte Ergebnisse, Speichern ganz ohne Kategorie (nur Stichwort, `category: null`), Löschen (optimistisch + Backend-Bestätigung), "Annuler" verwirft das Formular sauber, leerer Zustand. Keine Crashes im Logcat.
11. ✅ Benachrichtigungen + Push (FCM) — zwei Teile:
    - **In-App-Benachrichtigungen** (voll funktionsfähig, kein Backend-Vorbehalt): `ui/screens/notifications/`, Route `notifications`. CRUD gegen `GET/PATCH /api/notifications*` (neues `NotificationDto`/`NotificationRepository`). Glocke mit Badge in `HomeScreen`-TopAppBar (`BadgedBox`), Unread-Count wird alle 30s gepollt (`HomeViewModel.startUnreadPolling()`, mirrort den Web-Navbar-Poll — die Web-Seite hat ebenfalls keinen Socket-Live-Update für das Badge, nur die reine Chat-Notification kommt per Socket, aber selbst die aktualisiert im Web nicht das Badge). Klick-Routing pro Typ: `NEW_INQUIRY`/`NEW_REPLY`/`NEW_MESSAGE` (mit `conversationId`) → Chat, `NEW_REVIEW` → Profil, `SAVED_SEARCH_MATCH` (mit `listingId`) → Anzeigendetail, `REPORT_RESOLVED` → keine Navigation (Android nutzt `conversationId` direkt statt wie das Web über `listingId` eine Konversation zu suchen — einfacher, da das Feld ohnehin im Notification-Model existiert). Französische Templates hardcoded (kein i18n-Layer), 1:1 aus `fr.json` übernommen.
    - **FCM-Push**: Backend (`soukmar-backend`) um `FcmToken`-Prisma-Model + `POST /api/push/fcm-register`/`fcm-unregister` erweitert; `sendPushToUser` dispatcht jetzt parallel an Web-Push (Browser) UND FCM (nativ) — bestehende Call-Sites (Reviews, Saved-Search-Matches, Reports, Chat) mussten nicht geändert werden. `src/lib/fcm.ts` initialisiert `firebase-admin` lazy aus `FIREBASE_SERVICE_ACCOUNT`-Env-Var und no-opt silently, wenn die Var fehlt (aktuell der Fall — es existiert noch kein echtes Firebase-Projekt). Android-seitig: `com.google.gms.google-services`-Plugin + `firebase-messaging-ktx`, `SoukMarFirebaseMessagingService` (`fcm/`), Default-Notification-Channel in `SoukMarApplication`, Laufzeit-Berechtigungsanfrage für `POST_NOTIFICATIONS` (Android 13+) beim ersten Öffnen von `HomeScreen`, Token-Registrierung in `HomeViewModel.init` (per `FirebaseMessaging.getInstance().token.await()`, `kotlinx-coroutines-play-services`). **`app/google-services.json` ist aktuell ein Platzhalter mit Fake-Werten** (nur strukturell valide, damit der Build nicht bricht) — für echten Push muss ein reales Firebase-Projekt angelegt, die echte `google-services.json` hier eingespielt und `FIREBASE_SERVICE_ACCOUNT` im Backend-`.env` gesetzt werden. Bis dahin bleibt Push-bei-geschlossener-App unfunktional, aber alles andere (App offen/Hintergrund mit laufendem Prozess) funktioniert bereits vollständig getestet über die In-App-Liste.
    - Getestet end-to-end gegen den echten Backend-Server mit echten (aus früheren Phasen vorhandenen) Notification-Datensätzen: Badge zeigt korrekten Unread-Count sofort beim App-Start, alle 4 Notification-Typen korrekt gerendert (inkl. `SAVED_SEARCH_MATCH` mit Recherche-Name als `{name}`), Klick-Routing für alle Typen verifiziert (inkl. Graceful-Handling für zwischenzeitlich gelöschte Ziel-Annonce/Konversation aus alten Testdaten — kein Crash, sondern "introuvable"-Fallback), Mark-as-read (einzeln + "Tout marquer comme lu") serverseitig via curl bestätigt, **Live-Update-Test**: Bewertung von einem zweiten Account abgegeben während App offen war → Badge aktualisierte sich automatisch beim nächsten 30s-Poll ohne manuelles Neuladen. Laufzeit-Berechtigungsdialog erscheint korrekt. Keine Crashes im Logcat während der gesamten Session; `sendFcmToUser` no-opt bestätigt fehlerfrei ohne Firebase-Credentials (kein Fehler im Backend-Log).
12. ✅ Admin (Meldungen-Tab wie im Web) — `ui/screens/admin/`, Route `admin`, Einstieg im ⋮-Overflow-Menü von `HomeScreen` (ganz oben, Schild-Icon), nur sichtbar wenn `user.role == "ADMIN"` (mirrort exakt die Web-Navbar-Gating-Lücke: Backend/Guard erlauben zusätzlich `MODERATOR`, aber die Web-Nav zeigt den Link nur für `ADMIN` — Android übernimmt bewusst dieselbe Inkonsistenz statt sie zu "fixen", um Paritätsziel treu zu bleiben). **Scope-Entscheidung**: Nur der Signalements-Tab wurde portiert — die anderen 4 Web-Admin-Tabs (Aperçu/Annonces/Utilisateurs/Revenus) sind entweder rein client-seitig berechnet (kein Backend) oder rufen `/admin/users`-Endpoints auf, die serverseitig **gar nicht existieren** (404, Web fällt still zurück) — Meldungen ist der einzige Tab mit echtem, funktionierendem Backend-Contract, passend zum Phasennamen. CRUD gegen `GET /api/reports/admin` + `PATCH /api/reports/admin/{id}` (neues `AdminReportDto`/`AdminRepository`; `TokenManager` um `currentUserRole()`/`roleFlow` ergänzt, war bisher nur geschrieben, nie gelesen). Filter-Pills mit Live-Zählern (En attente/Résolu/Rejeté/Toutes), Default-Filter "En attente" wie im Web. Resolve/Rejeter öffnet einen echten Compose-`AlertDialog` mit optionalem Notiz-Textfeld (**bewusst kein JS-`prompt()`-Äquivalent** wie im Web — dort ist es ein Browser-`prompt()`, in einer nativen App gibt es dafür kein Pendant, ein Dialog ist die korrekte native Entsprechung). **Bug gefunden und gefixt**: `PATCH /reports/admin/:id` liefert im Response-Objekt keine `reporter`/`reported`/`listing`-Relationen (nur `GET` inkludiert diese) — ein naives Ersetzen des Listenelements mit der PATCH-Antwort hätte Namen/E-Mail/Anzeigenbezug nach jeder Resolve/Dismiss-Aktion auf "?" zurückgesetzt; behoben durch selektives Feld-Merge (`status`/`adminNote`/`resolvedAt` aus der Antwort, Rest vom bereits geladenen Datensatz). Getestet end-to-end mit echten, aus früheren Phasen vorhandenen Report-Datensätzen (1 PENDING, 1 RESOLVED) gegen den echten Backend-Server: Negativ-Test (USER-Account sieht keinen Admin-Eintrag) und Positiv-Test (ADMIN-Account, Demo-Account `youssef@soukmar.ma` ist bereits ADMIN) beide verifiziert, Filter-Pills mit korrekten Live-Zählern, Resolve-Flow mit Notiz UND Dismiss-Flow ohne Notiz (optionales Feld) beide bestätigt inkl. Backend-Abgleich per curl, Auslösen der bereits aus Phase 11 vorhandenen `REPORT_RESOLVED`-Notification für den Melder verifiziert (Notification-Count beim Reporter stieg nach Aktion), Anzeigen-Link in Report-Karte navigiert korrekt zur Detailseite. Keine Crashes im Logcat.
13. ✅ i18n-Schicht (6 Sprachen wie Web) — **Architektur**: statt 6×~880 Keys manuell in Kotlin nachzubauen, werden die Web-JSON-Dateien (`soukmar/src/assets/i18n/{fr,en,ar,de,es,it}.json`) 1:1 unverändert als App-Assets übernommen (`app/src/main/assets/i18n/`) und zur Laufzeit geparst — Single Source of Truth bleibt das Web-Repo, kein manueller Doppelpflege-Aufwand. Neues `I18nRepository` (`data/i18n/`) lädt alle 6 JSON-Dateien lazy, hält die aktuelle Sprache als Compose `mutableStateOf` (reaktiv, kein Neustart nötig — spiegelt exakt Webs Signal-basiertes `I18nService`), spiegelt Webs Lookup-Semantik 1:1: gepunkteter Key → verschachtelte Objekt-Traversierung, `{param}`-Interpolation (einfache geschweifte Klammern, kein `{{ }}`), fehlender Key → roher Key wird zurückgegeben (kein Fallback auf Französisch pro Key, nur global beim Start). Sprachauswahl persistiert in eigenem `LocalePreferences`-DataStore (`soukmar_prefs`, getrennt von `TokenManager`s `soukmar_session` — überlebt bewusst den Logout, wie Webs `soukmar_lang`-localStorage-Key unabhängig vom Auth-State ist). Zugriff aus Composables über `t("namespace.key")`/`t("key", "param" to wert)` (globaler Helper in `ui/i18n/LocalI18n.kt`, liest über `CompositionLocalProvider` am `MainActivity`-Root bereitgestelltes `I18nRepository`) sowie `tCatalog("cats.$code", code)` für Backend-getriebene Katalog-Codes (Kategorien/Subkategorien/EAV-Attribute/-Optionen) mit Fallback auf das alte `humanizeCode()` bei fehlendem Key. **RTL**: `LocalLayoutDirection` wird am `MainActivity`-Root reaktiv auf `Rtl` gesetzt, wenn `lang == "ar"` — Compose spiegelt darüber automatisch die gesamte UI (TopAppBar-Icons, Grid-Reihenfolge, FAB-Position etc.), exakt wie Webs `document.dir = 'rtl'`. Sprachumschalter (`ui/components/LanguageSwitcher.kt`, Flagge+Code+Dropdown, alle 6 Sprachen mit denselben Flaggen/Labels wie im Web-Navbar inkl. 🇲🇦 für Arabisch) sitzt in `HomeScreen`s TopAppBar und zusätzlich auf `LoginScreen` (damit auch nicht eingeloggte Nutzer vor dem Login die Sprache wählen können — im Web ist der Switcher überall sichtbar, da er Teil der globalen Navbar ist; Android hat keine globale Chrome, daher an den zwei wichtigsten Eintrittspunkten platziert statt auf jedem einzelnen Screen).
    - **Migrationsumfang**: alle 22 Screen-/Komponenten-Dateien mit UI-Text migriert (~320 `Text()`-Aufrufe), inkl. aller dynamischen EAV-/Katalog-Lookups (`humanizeCode()`-Aufrufe für Kategorien/Subkategorien/Attribute/Optionen vollständig durch `tCatalog()` ersetzt) und der lokalisierten relativen Zeitangabe (`timeAgoT()` in `ui/i18n/LocalI18n.kt`, nutzt `common.ago`/`hours`/`days`/`minutes` aus den JSON-Dateien; die Sub-Minute-"gerade eben"- und "Monate"-Fälle bleiben mangels JSON-Key in jeder Sprache Französisch — Web hat dafür auch keinen Key, da es sich auf `Intl.RelativeTimeFormat` verlässt, was Android nicht zur Verfügung steht).
    - **Bekannte, bewusst dokumentierte Lücken** (kein Blocker, siehe unten): (1) Backend-Fehlermeldungen bleiben unübersetzt Französisch — das Backend liefert grundsätzlich nur rohe französische Strings zurück (kein `Accept-Language`, keine `User.locale`-Spalte), ein Mapping hunderter Backend-Fehlertexte auf Keys wäre fragil und war explizit außerhalb des Scopes dieser Phase. (2) Vereinzelte Strings ohne Web-Pendant bleiben hardcoded Französisch (z. B. die "Bonjour, {name}"-Begrüßung auf `HomeScreen`, ein paar TopAppBar-Titel wie "Annonce"/"Profil vendeur" ohne passenden Key, "Retour"-contentDescriptions durchgängig — im Web-`fr.json` existiert dafür keine generische `common.back`-Übersetzung). (3) `profil.role_admin`-Wert ist im JSON `"🛡️ Admin"` statt des bisherigen Android-Textes "Administrateur" — sichtbare, aber Web-treue Änderung.
    - **Umsetzung**: Kern-Infrastruktur + die Screens mit dynamischen Katalog-Lookups (`HomeScreen`, `ListingsScreen`, `ListingDetailScreen`, `ListingCard`) direkt umgesetzt; die übrigen 13 Screens (Auth×3, Chat×2, Mes-Annonces/Favoris/Saved-Searches/Profil/Seller-Profile×5, Notifications+Admin×2, Deposer-Annonce) über parallele Subagenten migriert und anschließend zentral nachbereinigt (u. a. verbleibende `.label`/`humanizeCode()`/`timeAgo()`-Reste in `DeposerAnnonceScreen`, `MesAnnoncesScreen`, `SavedSearchesScreen` gefunden und gefixt, die die Agenten übersehen hatten, da sie außerhalb direkter `Text()`-Aufrufe lagen). Ein Subagent lief unerwartet in einem isolierten Git-Worktree (Auth-Screens) — dessen Änderungen wurden manuell zurück ins Hauptverzeichnis kopiert; mehrere Agenten liefen zwischenzeitlich mit parallelen, kollidierenden Gradle-Builds (IO-Race auf `app/build/`) — behoben durch gezieltes Stoppen der hängenden Agenten-Tasks und einen sauberen Einzel-Build.
    - **Sehr gründlich getestet** im Emulator gegen den echten Backend-Server: Sprachumschalter mit allen 6 Sprachen durchgeklickt, **sofortige reaktive Aktualisierung ohne Neustart** bestätigt (Home-Screen, Kategorien-Grid, Suchplatzhalter, FAB), **vollständiges RTL-Mirroring bei Arabisch** verifiziert (TopAppBar-Icons, Grid-Leserichtung, FAB-Position — alles korrekt gespiegelt, und korrekt zurück auf LTR bei Sprachwechsel), Deutsch-Test über mehrere Screens (Home, Anzeigenliste inkl. Filter-Panel mit Sous-catégorie/Marke/Erstzulassung-EAV-Attributen, Notifications mit `{name}`-Interpolation, Admin-Dashboard mit Status-Filtern), **Sprachauswahl übersteht App-Neustart** (DataStore-Persistenz bestätigt), keine Crashes im Logcat während der gesamten Session.
14. ⬜ iOS-App (später, braucht Mac oder Cloud-CI wie Codemagic)

**Jede Phase muss getestet werden**, bevor die nächste beginnt (Nutzer-Vorgabe) — Claude übernimmt das Testen selbst (Build + Emulator/adb), nicht der Nutzer manuell. Siehe Abschnitt "Testen" unten.

### Web-Sync-Runden (nach Phase 13, laufend)

Ab hier wächst die App nicht mehr in nummerierten Phasen, sondern in gebündelten **Tranchen**, die neue Web-Features nachziehen (siehe `[[feedback_soukmar_platform_sync]]`-Memory: "Web zuerst, Android in Runden nachziehen"). Jede Tranche: Web-Commit-Log seit dem letzten Sync durchsehen, pro Feature prüfen ob es für eine native App überhaupt Sinn ergibt (SSR/Open-Graph/etc. sind web-only), fehlende Features bauen, im Emulator gegen den echten Backend-Server end-to-end testen, dann committen+pushen.

- **Tranche 1**: 7 neue Kategorien, internationaler Telefon-Vorwahl-Picker (`ui/components/PhoneInput.kt` + `ui/model/DialCodes.kt`, 196 Länder), Passwort-Sichtbarkeits-Toggle mit Auto-Ausblenden (`AppTextField` in `CommonComponents.kt`), Kontotyp-Auswahl bei der Registrierung, Mes-Annonces-Sortierung + Ablaufdatum-Anzeige + Verlängern-Button (spiegelt Backends `expiresAt`/`expiryExtended`).
- **Tranche 2**: Nutzer im Chat blockieren/entblocken, Bewertungssterne im Chat-Header, Telefonverifizierung per SMS im Profil, vier Rechtstexte-Screens über einen generischen `LegalPageScreen` (Namespace+Sektionsanzahl, Inhalt rein aus den geteilten i18n-Assets), Standort-Karte auf der Anzeigendetailseite + Kartenansicht für die Anzeigensuche (neue `osmdroid`-Abhängigkeit, `ui/components/ListingsMapView.kt`).
- **Tranche 3**: Beim Scoping entdeckt und behoben — `AttributeField`s `when (def.type)`-Renderer in `DeposerAnnonceScreen.kt` hatte **keinen Fall für `MULTI_SELECT`/`DATE`**, wodurch Pflichtattribute dieser Typen (z. B. `SUBJECT` bei Cours & Leçons, `EVENT_DATE` bei Billets & Tickets) im Erstellungsassistenten gar nicht darstellbar waren — Anzeigen dieser Kategorien konnten so über Android nicht veröffentlicht werden. Behoben end-to-end: MULTI_SELECT als Checkbox-Liste (`attrMultiValue()`/`toggleAttrMulti()` im ViewModel), DATE über natives Material3 `DatePickerDialog` (Overlay-Klick-Pattern wie bei `CityDropdown`, siehe oben). Dabei zusätzlich einen echten Daten-Bug in `applyListingToForm()` gefunden und gefixt: mehrere `ListingAttributeValue`-Zeilen mit demselben Code (so speichert das Backend MULTI_SELECT — eine Zeile pro gewählter Option) wurden beim Laden einer Anzeige zum Bearbeiten durch `.associate{}` stillschweigend auf die letzte Zeile reduziert; jetzt korrekt per `groupBy` zusammengeführt. Gleiches Gruppierungsproblem auch auf der Anzeigendetailseite behoben (`ListingDetailScreen.kt`s `SpecRow` zeigte vorher eine Zeile pro MULTI_SELECT-Wert statt einer gemeinsamen Zeile). Neuer `dateAttrT()`-Helper (`LocalI18n.kt`) formatiert DATE-Attributwerte locale-abhängig via `DateTimeFormatter.ofLocalizedDate(MEDIUM)` (mirrort Webs `Intl.DateTimeFormat`). Außerdem: Abbrechen-Bestätigungsdialog beim Verlassen des Anzeige-Assistenten mit ungespeichertem Fortschritt (`requestClose()`/`hasProgress`, mirrort Webs `cancel()`), sowie 317 arabische Stadtnamen (`MOROCCO_CITIES_AR`, 1:1 aus `listing.model.ts` übernommen) über neuen `cityLabelT()`-Helper an allen Stadt-Anzeigepunkten (ListingCard, Mes Annonces, Verkäuferprofil, Anzeigendetail) verdrahtet. End-to-end gegen den echten Backend-Server getestet: MULTI_SELECT-Anzeige erstellt+veröffentlicht (Mathématiques+Physique angehakt) → Detailseite zeigt korrekt eine gemeinsame "Matière: Mathématiques, Physique"-Zeile; DATE-Anzeige erstellt+veröffentlicht (Concert-Test, `EVENT_DATE`=2026-10-15) → Detailseite zeigt "15 oct. 2026" (Französisch) bzw. "2026/10/15" (Arabisch, Java-ICU-Standardformat für `ar-MA`/MEDIUM) statt der rohen ISO-Zeichenkette; Abbrechen-Dialog ausgelöst und mit "Oui" bestätigt, korrekte Rücknavigation; arabische Stadtnamen auf Mes-Annonces-Liste und Anzeigendetail verifiziert (الرباط/الدار البيضاء/مراكش/فاس). Keine Crashes im Logcat. Zurückgestellt (nicht Teil dieser Tranche): Berufsvorschläge-Autocomplete nach Branche (`TextAutocompleteComponent`/`JOB_PROFESSIONS_BY_SECTOR`) — größerer, separater UI-Komponenten-Port.
- **Tranche 4**: Alle 5 Backend-Endpoints existierten bereits fertig, nur die Android-Seite fehlte. (1) **Konto-Löschung**: neuer `ui/screens/settings/DeleteAccountScreen.kt` (Passwort-Reeingabe + Bestätigungs-Checkbox, `DELETE /auth/account` über `@HTTP(method="DELETE", hasBody=true)`, da Retrofits `@DELETE` keinen Body direkt unterstützt), anonymisiert serverseitig Name/E-Mail/Telefon/Bild und schließt aktive Anzeigen. (2) **Gespeicherte Suchen bearbeiten**: neuer `updateSavedSearch()`-Endpoint (`PATCH`), `ListingsViewModel.editSearchId`-State schaltet `saveSearch()` zwischen POST (neu) und PATCH (Update) um; Einstieg über neuen Stift-Button in `SavedSearchesScreen.kt` neben dem bestehenden Lösch-Button. (3) **Ähnliche Anzeigen**: `GET /listings/:id/similar` auf der Anzeigendetailseite, wiederverwendet `ListingCard` in Row-Paaren (kein LazyVerticalGrid, da die Detailseite selbst schon eine scrollende Column ist). (4) **Bildkomprimierung vor Upload**: `UploadRepository.kt` skaliert jetzt via `BitmapFactory`/`inSampleSize` vor dem Upload herunter (Listing-Fotos max. 1600px, Avatar max. 640px, JPEG Qualität 82) und schickt neu ein `type`-Multipart-Feld (`listing`/`avatar`) mit — vorher lief jeder Avatar-Upload versehentlich durchs `listing`-Cloudinary-Preset (1200px-Limit-Crop) statt durchs `avatar`-Preset (400×400 Face-Crop), ein zusätzlich gefundener Bug. (5) **Personalisierte "Vos centres d'intérêt actuels"-Sektion** auf der Startseite (`GET /listings/interests`, Top-3 recency-gewichtete Kategorien). (6) Neuer zentraler **`SettingsScreen`** bündelt Profil/Notifications/Rechtstexte/Konto-löschen/Logout (vorher verstreut im ⋮-Overflow-Menü von `HomeScreen`) — Premium/Aide-Seiten des Webs bewusst nicht mitgebaut (Marketing-/Zahlungs-Inhalte ohne Android-Gegenstück, außerhalb des Scopes). End-to-end gegen den echten Server getestet: Konto-Löschung mit Fehlerfall (falsches Passwort) und Erfolgsfall (inkl. serverseitiger Verifikation der Anonymisierung über einen Wegwerf-Testaccount, um die echten Demo-Accounts nicht zu gefährden), Bearbeiten einer gespeicherten Suche (Name geändert, Backend-Log bestätigt PATCH statt neuem Eintrag), Ähnliche-Anzeigen-Navigation, Bild-Upload mit einem künstlich erzeugten 4000×3000px-Testfoto (Cloudinary lieferte danach 1200×900px aus — Downsampling bestätigt). Emulator-Sandbox war während dieser Tranche mehrfach durch parallele Host-Prozesse (Chrome/Edge, andere Claude-Sessions) für Minuten bis fast eine Stunde system-weit eingefroren (ANR) — jeweils durch Emulator-Neustart behoben, kein App-Fehler.
- **Tranche 5**: (1) **Job-Berufsvorschläge-Autocomplete** (in Tranche 3 zurückgestellt): neue wiederverwendbare `ui/components/TextAutocompleteField.kt` (diakritik-insensitive Filterung via `java.text.Normalizer`, `ExposedDropdownMenuBox` als Basis) mirrort Webs `TextAutocompleteComponent` exakt — freier Text bleibt immer editierbar, Vorschläge sind nur eine Abkürzung, ausgewählt wird das übersetzte Label (nicht der Rohcode), genau wie im Web. Neue `JOB_PROFESSIONS_BY_SECTOR`/`JOB_PROFESSION_CODES`-Konstanten in `CatalogModels.kt` (1:1 aus `listing.model.ts`, ~150 Berufscodes in 18 Branchen). Eingebunden an den zwei Stellen wie im Web: `DeposerAnnonceScreen.kt`s `AttributeField` (Sonderfall für `PROFESSION`, gefiltert nach dem aktuell gewählten `INDUSTRY`-Wert) und `ListingsScreen.kt`s Filter-Sidebar (neuer `attrTextFilters`-State in `ListingsViewModel`, gefiltert nach den per Chips ausgewählten Branchen). (2) **Kontotyp (Privat/Gewerblich) vervollständigt**: war bisher nur bei der Registrierung erfasst. Neue `AccountTypeLabel`-Composable (`CommonComponents.kt`, Icon+Text) an allen Stellen verdrahtet wo das Web es zeigt — Verkäuferprofil, Anzeigendetail-Verkäuferkarte —, `accountType`-Feld zu `ListingUserDto`/`SellerProfileDto` ergänzt. Bearbeitbar im eigenen Profil (`ProfilScreen.kt`, wiederverwendet die aus der Registrierung bekannte `AccountTypeSelector`-Komponente, `PUT /auth/profile` akzeptierte das Feld serverseitig bereits). Neuer, immer sichtbarer "Type de compte"-Filter (Particulier/Professionnel-Chips) in der Anzeigen-Filter-Sidebar (Backend-Query-Param `accountType`, kein `attr_`-Präfix). **Bug gefunden und gefixt** (`soukmar-backend/src/routes/auth.ts`): der `POST /auth/login`-Endpoint hatte `accountType` nicht im Prisma-`select` — jeder Login lieferte serverseitig einen `undefined` Kontotyp zurück, unabhängig vom tatsächlich gespeicherten Wert (nur `/me` und `/profile` hatten das Feld korrekt drin). Getestet end-to-end gegen den echten Server: Job-Autocomplete mit Branchenfilterung (Eingabe "web" ungefiltert vs. nach Wahl von "Informatique & Numérique" nur noch IT-Berufe), Kontotyp-Filter in beide Richtungen (Professionnel/Particulier blendet die einzige Demo-Job-Anzeige korrekt ein/aus, da der Demo-Account PRIVATE ist), Kontotyp-Änderung im eigenen Profil inkl. serverseitiger Verifikation über einen direkten Login-Aufruf (bestätigte gleichzeitig den Login-Bugfix) und Badge-Aktualisierung auf einer fremden Anzeigendetailseite, danach zurück auf PRIVATE gesetzt um den Demo-Account-Ausgangszustand nicht zu verändern. **Emulator-Testing-Lektion**: `ExposedDropdownMenu`s Popup-Fenster nimmt offenbar den Eingabefokus für Hardware-Keyevents (Backspace) an sich, sobald es offen ist — wiederholte `adb shell input keyevent 67`-Aufrufe an einem befüllten `TextAutocompleteField` löschten nur das allererste Zeichen zuverlässig, der Rest ging ins Leere, bis das Popup per System-Zurück explizit geschlossen wurde (Feld blieb danach fokussiert, keine Notwendigkeit erneut zu tippen) — vor jedem Lösch-Versuch an einem Autocomplete-Feld zuerst das Popup schließen.
- **Tranche 6**: Drei kleinere, unabhängige Web-Nachzügler. (1) **`NO_CONDITION_SUBCATEGORIES`**: neue `Set<String>`-Konstante in `CatalogModels.kt` (aktuell nur `TRAINING_OFFERS` unter Sport & Loisirs) für Subkategorien, die zwar in einer `CONDITION_CATEGORIES`-Kategorie liegen, aber Dienstleistungen statt physische Güter sind und daher kein Neuf/Occasion-Feld zeigen sollen — mirrort Webs gleichnamige Konstante. Eingebunden in **beide** Stellen, die bisher nur pauschal nach Kategorie prüften: `DeposerAnnonceViewModel.showCondition` (Anzeige-Assistent) und neu `ListingsViewModel.showCondition` (Such-Filter-Sidebar, `ListingsScreen.kt` nutzte vorher eine reine Kategorie-Prüfung direkt inline). (2) **Kategoriespezifische Platzhaltertexte** im Anzeige-Assistenten: Titel-/Beschreibungsfelder in `DeposerAnnonceScreen.kt` zeigen jetzt `t("deposer.placeholder_title_<category>")`/`placeholder_desc_<category>` (dynamischer Key aus der gewählten Kategorie, klein geschrieben) — alle 18 Keys waren durch den Phase-13-i18n-Sync bereits in den JSON-Assets vorhanden, es fehlte nur die UI-Verdrahtung. (3) **Verifiziert-Badge im Chat**: `VerifiedBadge`-Composable (aus Tranche 1) jetzt zusätzlich in `ChatListScreen.kt`s `ConversationRow` (vor dem Anzeigentitel) und `ChatScreen.kt`s `ChatHeaderContent` (nach Name+Sternebewertung) eingebunden, via bereits aus Tranche 2 vorhandenem `ConversationDto.partnerUser(myId)`/`ChatUserDto.emailVerified/phoneVerified` — reine UI-Ergänzung, keine neue Datenanbindung nötig. Getestet end-to-end gegen den echten Server: Sport & Loisirs → Offres d'entraînement sowohl im Anzeige-Assistenten als auch in der Such-Filter-Sidebar ausgewählt → Feld "État" verschwindet korrekt an beiden Stellen (Web-Parität), während bei Équipement sportif/Vélos weiterhin sichtbar; kategoriespezifische Platzhalter für Sport & Loisirs live im Titel-/Beschreibungsfeld bestätigt; Verifiziert-Badge ("✔ E-mail") erscheint korrekt auf allen vier Demo-Konversationen in der Chat-Liste UND im Chat-Detail-Header. Keine Crashes im Logcat. Emulator-Sandbox war während der Testing-Session mehrfach system-weit eingefroren (ANR, u. a. mit `qemu-system-x86_64` selbst als Top-CPU-Verbraucher) — jeweils durch wiederholtes "Warten"-Antippen bzw. einen App-Neustart behoben, kein App-Fehler.
- **Tranche 7**: Beim Scoping wurde der Web-Commit-Log seit Tranche 6 durchgesehen (u. a. eine "Rebrand-Feinschliff SouqMar24"-Serie und diverse kleinere UI-Politur-Commits) — die meisten Punkte (Betrugsmeldung Käufer/Verkäufer, diverse Icon-Professionalisierungen) stellten sich bei genauerer Prüfung als bereits erledigt heraus: Android nutzte für Melden/Kontotyp/Standort von Anfang an Material-Icons statt Emoji, und der Report-Dialog meldet bereits sowohl auf der Anzeigendetailseite als auch im Chat-Header die jeweils andere Partei. Drei echte Lücken blieben: (1) **Rebrand SoukMar → SouqMar24**: Web hat den App-Namen in Navbar/Footer/Auth-Seiten/Titel umbenannt (schwarzes "SouqMar" + rotes "24", 23 Vorkommen bereits korrekt in den seit Phase 13 verbatim kopierten i18n-Assets enthalten). Auf Android betraf das nur zwei hartcodierte Stellen außerhalb der i18n-Schicht: `strings.xml`s `app_name` (Launcher-Label) und die gemeinsame `SoukMarLogo`-Composable (`CommonComponents.kt`, genutzt von Home/Login/Register/Passwort-Screens) — Paket-/Klassennamen (`com.soukmar.app`, `SoukMarApplication` etc.) bewusst unangetastet gelassen, da eine App-ID-Änderung ein eigenständiges, hochriskantes Vorhaben wäre und nicht Teil eines reinen Text-Rebrandings ist. (2) **Kategorie-Reihenfolge**: `OTHER`/"Autres" ans Ende der `CATEGORIES`-Liste in `CatalogModels.kt` verschoben (war vorher an Position 8) und `LESSONS_COURSES` an die vom Web vorgegebene Position zwischen `BABY_KIDS` und `PETS` gerückt — beides 1:1 aus `listing.model.ts`s aktueller Reihenfolge übernommen. (3) **Chat-Zeitstempel**: Datum+Uhrzeit stehen jetzt immer links neben jeder Nachricht (Text- und Angebots-Bubble), zweizeilig (Datum über Uhrzeit, `dd.MM.`/`HH:mm`) — vorher stand die Uhrzeit einzeilig über der Bubble, seitenabhängig links/rechts je nach Absender. `ChatScreen.kt`s `TextBubble`/`OfferBubble` wickeln die Bubble jetzt in eine `Row(verticalAlignment = Bottom)` mit der neuen `MsgTimeLabel`-Composable als erstem Kind — die äußere `Column` bleibt weiterhin sender-abhängig rechts/links ausgerichtet, aber innerhalb der Row steht das Zeit-Label immer zuerst (links), unabhängig davon ob die Nachricht eigene oder die des Partners ist, exakt wie Webs `flex-direction: row`-Umbau. Getestet end-to-end gegen den echten Server: Rebrand-Text ("S SouqMar24") korrekt im TopAppBar-Logo auf Home bestätigt; Kategorie-Reihenfolge auf der Home-Kachel-Übersicht durchgescrollt und bestätigt (Services→Bébé & Enfants→Cours & Leçons→Animaux→...→Déménagement→Autres); Zeitstempel-Layout sowohl bei einer Text-Nachricht (eigene, rot, rechtsbündig mit Label links) als auch bei einer Angebots-Karte (MacBook-Pro-Angebot, Youssef Amrani) bestätigt — kein Überlauf, korrekte zweizeilige Darstellung. Keine Crashes im Logcat. Emulator-Sandbox war während dieser Session ungewöhnlich hartnäckig eingefroren (mehrere aufeinanderfolgende "Pixel Launcher isn't responding"-ANRs über ca. 20 Minuten, `qemu-system-x86_64` erneut mit weitem Abstand höchster Host-CPU-Verbraucher) — half diesmal erst "App schließen" statt weiterer "Warten"-Versuche, danach lief die App wieder normal; einzelne `adb`-Befehle liefen wiederholt in den 30s-Timeout und mussten im Hintergrund abgewartet werden. Zurückgestellt (bewusst nicht Teil dieser Tranche): das große Web-Redesign von Kategorie-Emoji auf ein 11-teiliges SVG-Line-Icon-Set (`fe5ad55` u. a.) — rein kosmetisch, kein Feature-Parität-Risiko, und der Port nach Compose (ImageVector-Pfade statt Emoji) wäre ein eigener, größerer Aufwand für einen separaten Anlauf.
- **Tranche 8**: Web hat in zwei Commits (`398f680`, `408fd0c`) die verbleibenden funktionalen Emoji-Icons (📍 Standort, 💰 Angebot) durch echte SVG-Icons ersetzt (eigene `app-icon`-Komponente mit den Namen `location`/`private`/`business`/`report`/`offer` — `private`/`business`/`report` waren auf Android bereits seit Tranche 1/5 als Material-Icons umgesetzt, nur `location` und `offer` fehlten noch). Betroffen: `ListingDetailScreen.kt` (Verkäuferkarte), `SellerProfileScreen.kt`, `SavedSearchesScreen.kt` (Meta-Zeile dabei von einem einzelnen `joinToString`-Text auf eine `Row` mit Icon+Text umgebaut, da ein Vektor-Icon nicht in einen einzelnen `Text()`-String eingebettet werden kann — mirrort Webs eigenen `<span class="icon-label">`-Ansatz; dabei nebenbei `cityLabelT()` ergänzt, das hier seit Tranche 3 fehlte), `MesAnnoncesScreen.kt` (gleiches Row-statt-Text-Muster, 👁/🕐-Emoji bewusst unverändert gelassen wie im Web) für `location`, sowie `ChatScreen.kt` (Angebots-Button, Angebots-Panel-Icon, Angebots-Bubble-Label) für `offer` (`Icons.Filled.LocalOffer`). Zusätzlich Web-Commit `874658b` nachgezogen: `MACHINE_TYPE`-SELECT-Feld (Location de machines/Location-Kategorie) im Anzeige-Assistenten bekommt jetzt wie im Web ein passendes Emoji pro Option vorangestellt (neue lokale `MACHINE_TYPE_ICONS`-Map + `machineTypeIcon()`-Helper in `DeposerAnnonceScreen.kt`, bewusst nicht in die geteilten `attrs.opts.*`-Strings eingebaut, exakt wie Webs eigene Begründung: `OTHER` wird an vielen anderen Stellen ohne Icon verwendet). Getestet end-to-end gegen den echten Server: Angebots-Icon (Preisschild) korrekt in Eingabeleiste und Angebots-Bubble einer laufenden Konversation; Standort-Icon korrekt auf der Anzeigendetail-Verkäuferkarte und in "Meine Anzeigen" (keine Zeilenumbrüche/Überlauf trotz Row-Umbau); `MACHINE_TYPE`-Dropdown im Anzeige-Assistenten (Location → Louer une machine) zeigt alle 20 Optionen mit korrektem Emoji sowohl in der geöffneten Liste als auch im geschlossenen Feld nach Auswahl ("⛏️ Pelleteuse"); Abbrechen-Dialog aus Tranche 3 dabei erneut bestätigt funktionsfähig. Keine Crashes im Logcat. Emulator-ANR trat wieder auf ("Pixel Launcher isn't responding") — diesmal sofort per "App schließen" (statt erneutem Warten) behoben, entsprechend der in Tranche 7 gewonnenen Erkenntnis.
- **Tranche 9**: Das in Tranche 1-8 bewusst zurückgestellte Web-Redesign von Kategorie-Emoji auf ein Lucide-artiges SVG-Line-Icon-Set (`fe5ad55`) wurde nachgezogen, da nach Tranche 8 keine weiteren offenen Web-/Backend-Commits mit Android-Relevanz mehr übrig waren. Neue `ui/model/CategoryIcons.kt`: `CategoryIcon(category, tint, modifier)`-Composable zeichnet alle 18 Kategorie-Icons selbst auf einem `Canvas` (skaliert auf 24×24-Einheiten wie Webs SVG-viewBox), `<path>`-Elemente werden per `PathParser().parsePathString(d).toPath()` direkt aus den Original-SVG-`d`-Strings aus `soukmar/src/app/components/cat-icon/cat-icon.component.html` übernommen (inkl. Bézier-/Arc-Kommandos), `<rect>`/`<circle>`/`<line>`/`<polyline>` werden mit den nativen `DrawScope`-Grundformen nachgebaut — kein manuelles Nachzeichnen der Formen nötig, nur mechanische Übertragung. Eingebunden an genau den 4 Stellen, die auch das Web tatsächlich umgestellt hat (nicht an den bewusst emoji-belassenen Stellen wie Listing-Card-Badge, Anzeigendetail-Badge, Mes-Annonces, gespeicherte Suchen — dort blieb `CATEGORIES.emoji` unverändert, exakt wie im Web): `HomeScreen.kt` (Haupt-Kategorienraster + "Vos centres d'intérêt"-Sektion), `ListingsScreen.kt` (Kategorie-Filter-Chips — `FilterChipItem` bekam dafür einen `icon`-Slot statt der bisherigen Emoji-Text-Verkettung; "Toutes"-Chip verlor dabei sein Icon ganz, weil auch Webs `nav.all`-Link keins hat), `DeposerAnnonceScreen.kt` (Kategorie-Auswahlschritt). Getestet end-to-end im Emulator: alle 18 Icons im Home-Raster, in der Such-Filter-Chip-Zeile (klein, 15dp, inkl. Farbwechsel bei Auswahl) und im Anzeige-Assistenten korrekt gerendert, keine Clipping-/Skalierungsfehler, Klick-Handling auf den Kacheln funktioniert weiterhin (Icon ist nur eine visuelle Ebene, kein Klick-Blocker). Keine Crashes im Logcat. **Schwerer Emulator-Ausfall während dieser Tranche**: adb-Befehle (auch einfache `screencap`) hingen wiederholt >20 Minuten fest, obwohl `adb get-state` weiterhin sofort antwortete — löste sich nicht durch Warten auf; Nutzer explizit gefragt und auf "Emulator neu starten" entschieden (`Stop-Process qemu-system-x86_64` + `emulator -avd soukmar_test -no-snapshot-load`, dann per Polling auf `getprop sys.boot_completed == 1` gewartet, ca. 27×3s ≈ 80s Boot-Zeit). Direkt nach dem Neustart schlug die allererste `adb install` mit einer internen `PackageManagerInternal`-NullPointerException fehl (Systemdienste noch nicht vollständig hochgefahren) — ein einfacher Retry nach kurzer Pause hat gereicht.
- **Tranche 10**: **GPS-Umkreissuche + Distanz-Sortierung** — der letzte noch offene Punkt aus dem ursprünglichen Rückstand (Kartenansicht selbst war bereits in Tranche 2 umgesetzt; nur "Meinen Standort verwenden" + Radius + Distanz-Sortierung fehlten noch). Backend-Vertrag (`GET /listings?lat&lng&radius&tri=distance`, Haversine-Query + serverseitige Distanz-Sortierung) existierte schon vollständig — reine Client-Ergänzung, mirrort iOS' identische Tranche 19 vom selben Tag. `ListingsViewModel.kt`: neue `lat`/`lng`/`radius`/`locationLoading`/`locationError`-States, `selectRadius()`/`setLocation()`/`updateLocationLoading()`/`updateLocationError()`/`clearLocation()` (bewusst **nicht** `setRadius`/`setSort` genannt — das existierende `sort`/`radius`-Property hätte sonst einen JVM-Signatur-Konflikt mit dem synthetisierten Property-Setter erzeugt, `Platform declaration clash` beim Kotlin-Compiler; erst beim ersten `assembleDebug` gefunden und korrigiert). `buildParams()` um `lat`/`lng`/`radius` ergänzt. `FiltersPanel` um zwei neue Sektionen ergänzt: "Ville / Commune" (GPS-Button → nach Erfolg Radius-Chips 5–200 km + Lösch-Button, Fehlertext bei verweigerter/nicht verfügbarer Position) und "TRIER PAR" (Plus récent/Prix croissant/Prix décroissant/Distanz — Distanz-Chip nur sichtbar wenn ein Standort aktiv ist) — **dabei auffällig**: Android hatte zwar bereits ein `sort`-State + `tri`-Query-Param in `ListingsViewModel.kt`, aber nie eine UI dafür verdrahtet; jetzt im selben Zug nachgeholt.

  **Location-Backend-Wechsel während des Testens**: zunächst mit `com.google.android.gms:play-services-location` (`FusedLocationProviderClient`) gebaut — im Emulator lieferte das aber durchgehend keinen Fix, selbst mit `adb emu geo fix` und direkter Mock-Injektion in den rohen GPS-Provider via `adb shell cmd location providers set-test-provider-location` (root nötig + `appops set android/root android:mock_location allow`, siehe [[project_soukmar_android]]-Memory für die genaue Befehlsfolge). Grund: Play Services' Fused-Engine läuft über einen eigenen, von Google-Konto/Netzwerk abhängigen Stack, der in einer unangemeldeten Emulator-Sandbox grundsätzlich keinen Fix produziert — kein Bug im eigenen Code, aber auch kein Verlass für echte Budget-Geräte ohne aktives Google-Konto. Daraufhin auf die reine Plattform-**`LocationManager`**-API umgestellt (`requestSingleUpdate()` in eine `suspendCancellableCoroutine` gewrappt, GPS-Provider zuerst, Network-Provider als Fallback, 15s-Timeout) — funktioniert überall, keine Play-Services-Abhängigkeit nötig, Dependency wieder entfernt. Kernlektion: `LocationListener.onLocationChanged()` liefert nur *neue* Updates, nicht die bereits gecachte `last location` — ein Mock-Update muss *nach* dem Registrieren des Listeners gesendet werden, sonst wartet die Coroutine bis zum Timeout.

  **Vollständig End-to-End im Emulator verifiziert** (nach Root-Zugriff + korrekter Mock-Location-Injektion): Berechtigungsdialog erscheint und lässt sich erteilen; nach Standortermittlung erscheinen korrekt "Ma position actuelle" + Lösch-Button, die Radius-Chips (5–100 km sichtbar) und die neue "Distance"-Sortieroption; die Umkreisfilterung selbst arbeitet nachweislich korrekt (0 Treffer bei 10 km um die Mock-Koordinaten, Treffer inkl. der bekannten Marrakech-Villa bei 100 km); "Distance" als Sortierung ausgewählt ohne Absturz; die bereits zuvor bestätigten Preis-Sortier-Chips funktionieren unverändert. Keine Crashes im Logcat während der gesamten Session.

- **Tranche 11 (2026-09-19): Internationalisierung (globale Länderauswahl)** — größte Tranche bisher, zieht [[project_soukmar]]s frisch gebautes Feature nach (Web-Commits `f4092e0`, `94fd55f`, `55cc9a4`, `ec5c56b`, `f69023d`, `44482da`): das Land, in dem eine Anzeige steht/gesucht wird, ist nicht mehr hart Marokko.
  - **Datenschicht** (`ui/model/CountryModels.kt`, neu): `COUNTRIES` (193 Länder, Code/Kontinent/Währung) und `CITIES_BY_COUNTRY` (~30 kuratierte Länder) **automatisch aus `soukmar/src/app/models/country.model.ts` generiert** (Node-Einmalskript, kein Handabschreiben) — Web bleibt Single Source of Truth wie bei den i18n-JSONs. **Eigener Bug im Extraktionsskript gefunden und gefixt**: die erste Fassung matchte nur einfach gequotete TS-Strings (`'...'`) und verschluckte alle doppelt gequoteten Städtenamen mit Apostroph (`"St. George's"`, `"M'Sila"`, `"N'Djamena"`, `"Xi'an"` u. a.) — Regex auf beide Quote-Stile erweitert, danach 1:1-Abgleich gegen die Web-Quelle bestätigt. `countryFlag()` nutzt (anders als Web) einen einfachen berechneten Emoji-Flag statt eines gebündelten SVG-Sets — der Windows-Chrome-Renderbug, der Web dazu zwang, existiert auf Android nicht (Noto Color Emoji rendert Flaggen-Emoji zuverlässig, wie `LanguageSwitcher` schon vorher bewies). Ländernamen kommen wie bei Web ohne Übersetzungstabelle aus, hier via `java.util.Locale("", code).getDisplayCountry(uiLocale)` als JVM-Äquivalent von `Intl.DisplayNames`.
  - **`CountryRepository`/`CountryPreferences`** (`data/country/`, `data/local/`) mirrort `I18nRepository`/`LocalePreferences` 1:1: Compose-State, Default `"MA"`, persistiert. **Echter Bug gefunden und gefixt**: `CountryPreferences` deklarierte anfangs eine eigene `preferencesDataStore(name = "soukmar_prefs")`-Instanz — derselbe Dateiname, den `LocalePreferences` bereits nutzt. Jetpack DataStore lässt aber nur eine aktive Instanz pro Datei zu und wirft beim Start `IllegalStateException: multiple DataStores active for the same file` (App-Absturz sofort nach Splash-Screen). Fix: die geteilte Instanz (`Context.soukmarPrefsDataStore`) liegt jetzt einmalig in `LocalePreferences.kt` (als `internal`), `CountryPreferences` verwendet dieselbe Property statt eine zweite zu deklarieren. Auf einen ersten echten App-Start ohne gespeichertes Land hin fragt `CountryRepository` einmalig `https://ipapi.co/json/` ab (bewusst über einen **eigenen, nackten `OkHttpClient`**, nicht den App-weiten aus `NetworkModule` — der hängt sonst automatisch den eigenen API-Bearer-Token an jede Anfrage, auch an einen Drittanbieter) und übernimmt das erkannte Land, mirrort Webs `CountryService.detectCountryFromIp()`. `fun setCountry` löste erneut den aus Tranche 10 bekannten `Platform declaration clash` mit dem synthetisierten `var country`-Setter aus — auf `updateCountry` umbenannt.
  - **`CountrySwitcher`** (`ui/components/`, neu) mirrort `LanguageSwitcher`: Flagge+ISO-Code-Button (kompakt, wie `🇫🇷 FR`) öffnet ein nach Kontinent gruppiertes, durchsuchbares Dropdown mit vollen Ländernamen — eingebunden in `HomeScreen` (App-weiter Kontext, über `HomeViewModel`), `LoginScreen` (Land vor dem Einloggen wählbar, analog zum dort bereits vorhandenen `LanguageSwitcher`, über einen neuen `country`/`selectCountry()` in `LoginViewModel`) und in `ListingsScreen`s `FiltersPanel` (eigener `country`-State + `selectCountry()` in `ListingsViewModel`, löst `search()` erneut aus, `buildParams()` hängt `country` immer an).
  - **`DeposerAnnonceScreen`/`-ViewModel`**: der alte feste `MAD/EUR/USD`-Currency-Dropdown ist weg, ersetzt durch ein Pflichtfeld "Pays" (neuer `CountryDropdown`, gleiches Muster wie `CountrySwitcher`, aber mit vollem Namen im Feld selbst) direkt vor "Ville". `ListingFormState.derivedCurrency`/`citiesForCountry` leiten Preis-Label bzw. Städteliste aus `country` ab (`currencyForCountry()`), nicht mehr manuell gesetzt. `CityDropdown` generalisiert: `cities`-Parameter statt fix `MOROCCO_CITIES` — leere Liste (die ~163 nicht kuratierten Länder) lässt das Feld zum reinen Freitext werden (kein Dropdown-Overlay), exakt Webs `CountrySelectComponent`-Fallback. Neuer Listing bekommt beim Öffnen `countryRepository.country` als Default, ein bearbeiteter Listing sein gespeichertes `listing.country`. `ListingDto`/`ListingUpsertRequest` (`data/remote/dto/`) um `country` ergänzt; `ListingUpsertRequest` verliert sein `currency`-Feld komplett (Backend leitet es serverseitig aus `country` ab, liest kein Client-`currency` mehr, siehe Web-Backend-Tranche).
  - **Preisformatierung generalisiert** (`ui/model/CatalogModels.kt`): `formatPriceParts()` nutzte bisher `NumberFormat.getIntegerInstance(Locale.FRANCE)` und ignorierte `currency` komplett (hängte den Code nur als rohen String an). Jetzt `NumberFormat.getCurrencyInstance(localeForLang(lang))` mit echtem `Currency.getInstance(code)` — funktioniert automatisch für jeden ISO-4217-Code. `formatToCharacterIterator()` (JVM-Äquivalent von `Intl.NumberFormat.formatToParts()`) trennt Betrag von Währungssymbol/-code für die bestehende kleinere Währungs-Schriftgröße. Neue `formatPricePartsT()`-Composable-Hülle (`ui/i18n/LocalI18n.kt`, mirrort `dateAttrT()`/`timeAgoT()`) liest die aktive Sprache automatisch — an allen 4 Composable-Aufrufstellen (`ListingCard`, `ListingDetailScreen`, `MesAnnoncesScreen`, `ChatScreen`s Angebots-Bubble) eingesetzt; `ListingsMapView.kt`s Marker-Snippet bleibt beim Default `lang="fr"`, da es außerhalb eines Composable-Kontexts (AndroidView-`update`-Lambda) liegt. Der bisher hartcodierte Angebots-Eingabe-Platzhalter "Montant en MAD" im Chat ist jetzt währungsabhängig (`viewModel.conversation?.listing?.currency`), da er sonst bei einer EUR-Anzeige irreführend gewesen wäre.
  - **Ausländer-Flagge** neben der Stadt (mirrort Webs `foreignFlag`/`foreignCountry`, nur für Nicht-Marokko-Anzeigen): `ListingCard.kt` und `ListingDetailScreen.kt` zeigen jetzt `countryFlag(listing.country)` neben `cityLabelT()`, wenn `listing.country != "MA"`.
  - **i18n-Assets neu synchronisiert**: alle 6 `assets/i18n/*.json` 1:1 aus dem Web-Repo kopiert (u. a. `deposer.label_country`, `deposer.region_asia/north_america/south_america/oceania`, geänderter `nav.topbar_tagline`, entfernter `home.hero_subtitle` — letzterer hat ohnehin kein Android-Pendant, Android hat keine Navbar/Hero-Sektion).
  - **Kein Web-Pendant, bewusst nicht portiert**: `home.hero_subtitle`/`nav.topbar_tagline`s dynamische Länderanzeige — reine Web-Navbar/Hero-Funktion, Android hat keine globale Chrome-Leiste dafür.
  - **Getestet im Emulator** gegen den echten Backend-Server (nach Fund+Fix des DataStore-Absturzes): `CountrySwitcher` auf Home öffnet korrekt gruppiert/durchsuchbar (🇲🇦 Maroc, 🇩🇿 Algérie, 🇹🇳 Tunisie, … mit korrekten französischen Namen); `DeposerAnnonceScreen`: Land-Wechsel Maroc→Algérie aktualisiert live "Prix (MAD)"→"Prix (DZD)" UND leert das Stadtfeld, Städte-Dropdown zeigt danach korrekt algerische Städte (Algiers/Oran/Constantine/Annaba/Blida); kein Absturz im gesamten Testlauf (`grep FATAL` im Logcat leer), App-Prozess durchgehend eine einzige PID. **Sandbox-Hinweis**: diese Session litt unter zwei aufeinanderfolgenden system-weiten ANRs (einmal die App selbst, einmal sogar System UI) während des interaktiven Testens — nicht app-bezogen (identisches Muster wie in Tranche 9 dokumentiert), behoben durch einen Emulator-Neustart; die eigentliche Funktionsverifikation gelang danach ohne weitere Probleme.

- **Tranche 12 (2026-09-19): Land in Anzeige aufgeben zentral steuern** — zieht einen Web-Nachbesserungs-Commit zu Tranche 11 nach: das Land war dort doppelt wählbar (Navbar UND ein eigenständiges, unabhängig änderbares Feld im Anzeige-Assistenten). Web hat das Formular-Feld ersatzlos entfernt — das Land wird jetzt ausschließlich zentral über die Navbar gesteuert. Android mirrort das: `CountryDropdown`-Composable und `DeposerAnnonceViewModel.selectCountry()` entfernt (beide waren nur für dieses eine, jetzt gestrichene Feld da). `form.country` bleibt für neue Anzeigen live mit `CountryRepository.country` synchron — vorher nur einmalig beim Öffnen des Assistenten gesetzt, jetzt über `snapshotFlow { countryRepository.country }.collect { ... }` in `DeposerAnnonceViewModel.init()` (Compose-Äquivalent von Webs `effect()`), sodass ein Länderwechsel über den `CountrySwitcher` auch bei bereits offenem Assistenten sofort Preis-Währung und Städteliste aktualisiert — ein `countrySyncStarted`-Flag verhindert mehrfaches Starten des Flows bei wiederholten `init()`-Aufrufen (Recomposition). Bearbeiten einer bestehenden Anzeige bleibt unverändert (übernimmt weiterhin `listing.country`, wird nicht vom Flow überschrieben, da der Sync-Zweig nur im reinen Neu-Erstellen-Pfad läuft). Getestet im Emulator gegen den echten Backend-Server: Anzeige-Assistent (Mode → Femme → Détails) zeigt jetzt direkt "Prix (MAD)" → "Ville" ohne dazwischenliegendes "Pays"-Feld, Abbrechen-Bestätigungsdialog aus Tranche 3 weiterhin funktionsfähig, keine Abstürze (`grep FATAL` im Logcat leer, App-Prozess durchgehend eine PID).

- **Tranche 13 (2026-09-20): Foto per Kamera aufnehmen** — bisher konnte man im Anzeige-Assistenten Fotos nur aus der Galerie wählen (`ActivityResultContracts.GetMultipleContents()`); die `CAMERA`-Permission stand zwar schon in `AndroidManifest.xml` (Altlast aus dem ursprünglichen Scaffold), wurde aber nirgends tatsächlich genutzt. Ergänzt um einen echten Kamera-Direktaufnahme-Weg neben dem Galerie-Weg, mirrort denselben Zug auf Web (`<input capture="environment">`) und iOS (`UIImagePickerController(sourceType: .camera)`). Neue `res/xml/file_paths.xml` + `<provider android:name="androidx.core.content.FileProvider">`-Eintrag in `AndroidManifest.xml` (Autorität `${applicationId}.fileprovider`, Cache-Pfad `camera/`) — nötig, weil `ActivityResultContracts.TakePicture()` anders als `GetMultipleContents()` selbst keine Uri liefert, sondern eine vom Aufrufer vorbereitete Ziel-Uri erwartet, in die die System-Kamera-App das Foto schreibt. `DeposerAnnonceScreen.kt`: neuer `cameraLauncher` (`TakePicture()`) + `startCameraCapture()` (legt eine Temp-Datei unter `cacheDir/camera/photo_<timestamp>.jpg` an, holt sich per `FileProvider.getUriForFile()` eine Content-Uri, merkt sie sich in `pendingCameraUri` und übergibt sie dem Launcher) + `launchCamera()` (prüft zuerst `ContextCompat.checkSelfPermission(CAMERA)`, fragt sonst per `RequestPermission()`-Launcher nach — exakt dasselbe Check-dann-Anfragen-Muster wie `ListingsScreen`s bestehender `ACCESS_FINE_LOCATION`-Flow, da manche Geräte `ACTION_IMAGE_CAPTURE` sonst stillschweigend verweigern, obwohl die eigene App die Kamera-Hardware nie selbst anspricht). Ergebnis-Callback ruft bei Erfolg `viewModel.addPhotos(listOf(uri))` — dieselbe, bereits generische Methode, die auch der Galerie-Picker nutzt, keine Änderung am ViewModel nötig. Neuer "📷 Prendre une photo"-Button (`PhotosStep`, `Icons.Filled.PhotoCamera`) direkt unter dem bestehenden Drag&Drop-Upload-Feld, respektiert dieselbe `maxPhotos`-Grenze. Neuer i18n-Key `deposer.take_photo` (aus dem Web-JSON-Bundle übernommen, alle 6 Sprachen). **End-to-end im Emulator gegen den echten Backend-Server getestet** (inkl. Runtime-Permission-Dialog "Allow SouqMar24 to take pictures and record video?" → "While using the app"): System-Kamera-App öffnet korrekt über den neuen Button, Aufnahme mit der virtuellen AVD-Kamera (Test-Haus-Bild) + Bestätigen-Häkchen funktioniert, aufgenommenes Foto erscheint korrekt im Foto-Grid mit "Principale"-Badge — identisches Verhalten zu einem Galerie-Foto. Keine Abstürze (`grep FATAL` im Logcat leer). **Sandbox-Hinweis**: mehrere ANRs während des interaktiven Testens (App und Eingabefelder reagierten träge, ein "SouqMar24 isn't responding"-Dialog erschien zweimal beim Städte-Dropdown) — konsistent mit den in Tranche 9/10 dokumentierten Sandbox-Lastproblemen (hohe Host-CPU-Auslastung durch parallele Prozesse), kein App-Fehler; ein App-Neustart (nicht Emulator-Neustart) hat diesmal gereicht.
- **Tranche 15 (2026-09-21): 5 neue Free-Tier-Features aus Web nachgezogen** — größte Tranche seit der i18n-Einführung, alle 5 Backend-Contracts existierten bereits fertig in `soukmar-backend`.

  **(1) Preis-Abfall-Benachrichtigungen**: Backend versendet bereits automatisch eine `Notification` mit `type: 'PRICE_DROP'`/`actorName: "-XX%"`, wenn eine favorisierte Anzeige im Preis fällt — kein neuer Endpunkt nötig. `NotificationsScreen.kt`s bereits generischer `templateFor()`/Klick-Router bekam zwei neue Fälle: `PRICE_DROP` (Text aus `notifications.price_drop`, Klick → `onOpenListing(n.listingId)`, exakt wie `SAVED_SEARCH_MATCH`) und nebenbei `ID_VERIFICATION_REVIEWED` (aus Feature 3 unten, gleiches Muster, sonst hätte der Typ in die `else`-Falle "Nouvelle notification." gefallen).

  **(2) Verkäufer-Conversion-Funnel**: neuer `GET /listings/:id/funnel` (Owner/Admin-only) → `{views, favorites, contacts, offers, offersAccepted}`. Neues `ListingFunnelDto`, `ListingRepository.getFunnel()`. `MesAnnoncesViewModel.toggleStats()` lädt jetzt parallel zur bestehenden 14-Tage-Statistik auch die Funnel-Daten (`funnelData`-Map, gleiches Lazy-Load-Muster), neue `funnelPct(id, count)` mirrort Webs `funnelPct()` (Bar-Breite relativ zu `views`, division-by-zero-safe). Neue `FunnelPanel`/`FunnelRow`-Composables in `MesAnnoncesScreen.kt` direkt unter dem bestehenden `StatsPanel` — 5 Zeilen (Label + proportionale Bar in `BorderColor`-Track/`Primary`-Fill, "Offres acceptées" in `SuccessColor` statt `Primary`, mirrort Webs `--accepted`-Grün) + Rohzahl rechts.

  **(3) ID-Verifizierung**: neues `User.idVerified: Boolean`-Feld — zu `ListingUserDto`, `UserDto`, `ChatUserDto`, `SellerProfileDto` ergänzt (direkt neben den bereits vorhandenen `emailVerified`/`phoneVerified`). `VerifiedBadge` (`CommonComponents.kt`) um einen dritten optionalen `idVerified`-Parameter erweitert (neue `seller.id_verified_short`-Pille) und an allen 4 bestehenden Call-Sites durchgereicht (`ListingDetailScreen`, `SellerProfileScreen`, `ChatScreen`, `ChatListScreen`). Neue **Nutzer-Seite**: `ProfilScreen.kt` bekommt eine neue "Vérification d'identité"-Karte (`IdVerificationCard`, unter der Passwort-Karte) mit 4 Zuständen (nicht eingereicht/PENDING/APPROVED/REJECTED-mit-Notiz-und-Neueinreichung-erlaubt, exakt wie `profil.component.html`s `@if`-Kaskade) — zwei Einzelbild-Picker (`ActivityResultContracts.GetContent()`, gleiches Muster wie der Avatar-Picker) laden über den bestehenden `UploadRepository.uploadImages(type = "idVerification")` zu Cloudinary hoch, dann `POST /auth/id-verification` mit den beiden URLs. Status-Check via neuem `GET /auth/id-verification` (liefert **`null`**, wenn nie eingereicht — `AuthRepository.getIdVerificationStatus()` gibt bewusst `ApiResult<IdVerificationStatusDto?>` zurück statt eines Fehlers, da ein `null`-Body hier kein Fehlerfall ist). Neue **Admin-Seite**: `AdminScreen.kt` bekommt eine zweite `TabRow`-Tab ("Vérifications" neben "Signalements", `AdminTab`-Enum) mit demselben Filter-Pillen-Muster wie der Signalements-Tab (Offen/Genehmigt/Abgelehnt/Alle — wiederverwendet bewusst die `admin.reports_status_*`-Keys für APPROVED/REJECTED, exakt wie Web es tut, statt eigene Keys zu erfinden), `GET/PATCH /admin/id-verifications*` (neues `AdminIdVerificationDto`/`AdminRepository`-Erweiterung), zwei Cloudinary-Foto-Thumbnails pro Anfrage (`AsyncImage`, keine lokale Speicherung nötig — reine URLs), Genehmigen/Ablehnen über denselben `AlertDialog`-mit-optionaler-Notiz wie beim Signalements-Tab.

  **(4) Bildsuche ("Rechercher par photo")**: neuer öffentlicher `POST /listings/search-by-image` (dediziertes Multipart-Feld `image`, **nicht** der allgemeine `/upload`-Endpunkt — der Server hasht das Foto nur lokal, ohne es dauerhaft zu speichern). Neues `ui/screens/imagesearch/` (`ImageSearchScreen`/`-ViewModel`), `UploadRepository.searchByImage()` (eigene Kompression/Multipart-Konstruktion, mirrort `uploadImages()`, aber gegen den separaten Endpunkt). Android hängt zusätzlich `?country=<CountryRepository.country>` an — bewusste Abweichung von Web (das den Query-Param serverseitig zwar unterstützt, aber selbst nie sendet), konsistent mit Androids eigener durchgängiger Landes-Scoping-Konvention seit Tranche 11. Einstiegspunkt: neuer Kamera-Icon-Button in `HomeScreen`s Suchleiste (rechts neben dem Platzhaltertext, außerhalb von dessen eigenem `clickable`, damit beide Taps unabhängig funktionieren — die Suchleiste war vorher eine einzige durchgehend klickbare `Row`, jetzt eine äußere `Row` mit einer inneren klickbaren `Row` für Text+Lupe plus einem separaten `IconButton`).

  **(5) Maskiertes In-App-Sprachanruf (WebRTC) — Machbarkeit vorab geprüft**: Googles `org.webrtc:google-webrtc` wird seit ~2023/24 nicht mehr zuverlässig auf Maven Central gepflegt. Recherche ergab `io.getstream:stream-webrtc-android` (Apache-2.0, aktiv gepflegter Fork mit vorgebauten AARs desselben nativen libwebrtc, Paketname bleibt `org.webrtc.*` für Drop-in-Kompatibilität) — Maven-Central-Metadaten bestätigten Version 1.3.10 (Sept. 2025) als aktuell gepflegt. **Go-Entscheidung bestätigt**: Dependency zu `app/build.gradle.kts` hinzugefügt, `./gradlew assembleDebug` lief sauber durch (inkl. korrektem Packaging der nativen `libjingle_peerconnection_so.so`, nur eine harmlose "Unable to strip"-Warnung). Signalisierung über den bereits bestehenden Chat-Socket (`ChatSocketManager` um `call_offer`/`call_answer`/`call_ice_candidate`/`call_end`-Emit-/Listen-Methoden + vier neue `ChatSocketEvent`-Varianten ergänzt, JSON-Form 1:1 wie `RTCSessionDescriptionInit`/`RTCIceCandidateInit` im Web, also interop-fähig). Neue `CallManager`-Klasse (`data/remote/`, **kein** `@Singleton` — bewusst unscoped, damit `ChatViewModel` pro Konversation eine frische `PeerConnection`-Instanz bekommt statt eine über Konversationen hinweg leckende globale) kapselt `PeerConnectionFactory`/`PeerConnection`-Lifecycle, mirrort Webs `ChatService`-Zustandsautomat (`CallPhase.IDLE/OUTGOING/INCOMING/ACTIVE`) 1:1 inkl. Auto-Decline bei bereits laufendem Call. Nur freie STUN-Server (`stun.l.google.com:19302`/`stun1...`), kein TURN-Relay, keine bezahlte Signalisierung — audio-only (kein Video-Track). UI in `ChatScreen.kt`: Anruf-Icon-Button im Header (nur sichtbar wenn nicht blockiert und `phase == IDLE`), `RECORD_AUDIO`-Laufzeit-Berechtigungsanfrage vor Start/Annahme (exakt dasselbe Check-dann-Anfragen-Muster wie Tranche 13s `CAMERA`-Flow), `IncomingCallDialog` (nicht wegwischbar, Annehmen/Ablehnen), `ActiveCallBar` (Gold während "sonnerie", Grün während aktivem Gespräch, Mute-Toggle, Auflegen-Button). Neue Manifest-Permissions `RECORD_AUDIO`/`MODIFY_AUDIO_SETTINGS`.
  **Nicht live im Emulator verifiziert (ehrlich dokumentiert, kein App-Fehler)**: Nach dem Testen der Features 1/2/4 und der Nutzer-Seite von Feature 3 (siehe unten) musste der Emulator aus anderen Gründen neu gestartet werden; danach geriet die Sandbox in einen außergewöhnlich schweren, lang anhaltenden ANR-Zustand — `system_server` blieb über 10 Minuten in Dauerlast, der `package`-Service war wiederholt nicht erreichbar (`Can't find service: package`, dann `PackageManagerInternal`-NullPointerException wie in Tranche 9, dann `Broken pipe`, dann Timeout), sodass nicht einmal ein simples `adb install` zuverlässig durchlief — deutlich schwerwiegender als die in Tranche 9/14 dokumentierten Fälle. Ein Zwei-Parteien-Anruftest (Android ↔ Web-Browser auf `localhost:4200`, beide mit echten Demo-Accounts und einer bestehenden Konversation — dafür war die Session bereits vorbereitet) konnte dadurch nicht mehr durchgeführt werden. Der Code gilt als bau- und architektur-korrekt (Signalisierungsvertrag 1:1 mit dem Backend/Web abgeglichen, `assembleDebug` erfolgreich), aber das tatsächliche Klicken durch Anruf-Button → Klingeln → Annehmen → Gespräch → Auflegen steht für eine künftige Session mit einem gesunden Emulator noch aus.

  **Live im Emulator gegen den echten Backend-Server getestet** (vor dem oben beschriebenen Emulator-Neustart): i18n-Assets aller 6 Sprachen neu synchronisiert (`suggest_in`/`search_by_image`, komplettes `image_search.*`, `chat.call_*`, `notifications.price_drop`/`id_verification_reviewed`, `admin.tab_id_verifications`/`id_verifications_*`/`reports_status_approved`/`reports_status_rejected`, `profil.id_verification_*`, `mes_annonces.funnel_*`, `seller.id_verified`/`id_verified_short`). **Preis-Abfall**: Ahmeds Testaccount favorisierte Fatimas BMW-Anzeige, Fatima senkte den Preis per API von 420 000 auf 300 000 MAD (-29 %) — Notification erschien sofort in der Liste ("Baisse de prix (-29 %) sur une annonce que vous suivez.", ungelesen/rot markiert), Klick navigierte korrekt zur Anzeigendetailseite mit dem neuen Preis. **Funnel**: "Statistiques"-Panel auf einer eigenen Anzeige (151 Aufrufe) zeigte den neuen Entonnoir-Block korrekt (Vues-Balken 100 % breit, restliche Zeilen 0 bei einer Anzeige ohne Favoriten/Kontakte) direkt unter der bestehenden 14-Tage-Grafik; Backend-Gegenprobe per curl auf einer Anzeige mit echtem Favoriten-Eintrag bestätigte korrekte Zähler (34 Aufrufe, 1 Favorit). **ID-Verifizierung (Nutzerseite)**: Ahmed wählte über die neue Karte ein ID-Foto und ein Selfie aus der Galerie, Einreichen lud beide zu Cloudinary hoch (`soukmar/id-verification/`-Ordner bestätigt) und rief `POST /auth/id-verification` auf — Karte zeigte danach korrekt "Votre demande est en cours d'examen." (PENDING), Zustand überlebte einen Screen-Neuaufbau (Navigation weg und zurück). **ID-Verifizierung (Admin-Contract)**: da die Admin-Tab-UI selbst wegen des Emulator-Ausfalls nicht mehr klickbar getestet werden konnte, wurde der komplette Backend-Vertrag, den `AdminScreen.kt`/`AdminViewModel.kt` konsumieren, direkt per curl gegen den echten Server verifiziert — `GET /admin/id-verifications` liefert Ahmeds PENDING-Antrag inkl. echter Cloudinary-URLs und `user`-Referenz; `PATCH .../APPROVED` setzt `idVerified: true` beim Nutzer (per erneutem Login bestätigt); eine zweite, für Fatima eingereichte Testanfrage wurde mit `PATCH .../REJECTED` + Notiz abgelehnt, `GET /auth/id-verification` lieferte danach korrekt `{status: "REJECTED", adminNote: "..."}` — exakt die Form, die `IdVerificationCard`s REJECTED-Zweig erwartet. **Bildsuche**: Foto aus der Galerie gewählt, `POST /listings/search-by-image?country=MA` lief in 1,2 s durch (200 OK, leeres Array — erwartungsgemäß, da das Testfoto ein App-Screenshot ohne Ähnlichkeit zu echten Anzeigenfotos war), Empty-State ("Aucune annonce similaire trouvée" + "Parcourir les annonces"-Button) korrekt gerendert. Keine Abstürze (`grep FATAL` im Logcat leer) während der gesamten Zeit vor dem Emulator-Neustart.

- **Tranche 16 (2026-09-23): Bezahlte Sichtbarkeits-Boosts** — zieht das neue Web-Feature nach (siehe `[[project_soukmar]]`): 4 eigene Stufen (Sofort-Bump/Spotlight/Top-Platzierung/Grenzüberschreitende Reichweite, bewusst kein 1:1-Kleinanzeigen-Klon), 10%-Kombi-Rabatt, Anfrage landet als PENDING (kein Zahlungsanbieter vorhanden) und wird von einem Admin manuell freigeschaltet. `ListingDto` um `boostSpotlightUntil`/`boostTopUntil`/`boostGlobalUntil`/`boostRank` ergänzt, neue `BoostDto.kt` (Tier-Konfiguration + `quoteBoostPrice()`, mirrort Webs `boost.model.ts`), zwei neue `ApiService`-Endpunkte (`GET .../boost-status`, `POST .../boost-request`) + `ListingRepository`-Methoden. Neuer Screen `ui/screens/boostlisting/` (`BoostListingScreen`/`-ViewModel`, Route `boost_listing/{listingId}`), neuer Rocket-Button in `MesAnnoncesScreen.kt`s Aktionsleiste. `ListingCard.kt` zeigt bei aktivem Boost ein "Top"/"Spotlight"-Badge + farbigen Rahmen (wiederverwendet `Primary`/`PrimaryLight`, keine neue Farbe). Admin bekommt einen dritten Tab "Boosts" (`AdminScreen.kt`/`AdminViewModel.kt`/`AdminRepository.kt`), identisches Filter-Pillen+Dialog-Muster wie die Vérifications-Tab. Neuer `NotificationType.BOOST_REVIEWED`-Fall in `NotificationsScreen.kt`. i18n aller 6 Sprachen aus dem Web-Repo übernommen. **Zwei echte Compiler-Fehler beim ersten `compileDebugKotlin` gefunden und gefixt**: `t(...)` (das `@Composable` `LocalI18n`-Kurzform) innerhalb eines `joinToString{}`-Lambdas aufgerufen — `joinToString`s `transform`-Parameter ist trotz `inline fun` nicht inlinebar (nullable Funktionstyp-Parameter werden vom Compiler automatisch `noinline`), daher kein `@Composable`-Kontext; behoben durch direkten `i18n.t(...)`-Aufruf auf der plain `I18nRepository`-Instanz statt der Composable-Hülle. Zweiter Fehler: `var showSelectOneError by remember { mutableStateOf(false) }` ohne `import androidx.compose.runtime.setValue` (nur `getValue` war importiert) — Property-Delegate für einen `var` braucht beide Operatoren. **`./gradlew assembleDebug` bestätigt BUILD SUCCESSFUL** (volle APK-Assemblierung, nicht nur Kotlin-Compile). **Interaktiver Emulator-Test nicht möglich**: `soukmar_test` stürzte beim Rendern mit einem Windows-GDI-Fehler ab ("Ein an das System angeschlossenes Gerät funktioniert nicht", `UpdateLayeredWindowIndirect failed`) — Gerät verschwand aus `adb devices`, bevor die App installiert werden konnte; nicht app-bezogen (identisches Muster zu den in früheren Tranchen dokumentierten Sandbox-Problemen), aber diesmal so früh, dass nicht einmal ein Login-Screenshot gelang. Der Code gilt als kompilier-korrekt (inkl. vollem Ressourcen-/Manifest-/Dex-Durchlauf), ein echtes Durchklicken (Boost auswählen → Anfrage senden → Admin genehmigt → Badge/Sortierung ändern sich) steht für eine künftige Session mit gesundem Emulator noch aus.

- **Tranche 14 (2026-09-20): Icon-Professionalisierung** — App-weiter Audit (mirrort denselben Zug auf Web und iOS) fand rohe `Text("<emoji>")`-Aufrufe als funktionale UI-Icons, obwohl Tranche 8/9 bereits `Icon(Icons.Filled...)`/`CategoryIcon` als etabliertes Icon-System eingeführt hatten — bewusst NICHT angetastet: `CategoryConfig.emoji`, `countryFlag()`, `★`/`☆`-Bewertungsglyphen, `MACHINE_TYPE_ICONS`, große dekorative Empty-State-Icons (40sp in Gate-/Erfolgs-/Leer-Screens) und i18n-String-eigene Emoji-Präfixe (z. B. "📸 Ajouter des photos", gemeinsam mit Web/iOS aus derselben JSON). Ersetzt durch Material Icons: `ListingCard.kt`s Premium-Badge (⭐→`Icons.Filled.Star`, höchste Sichtbarkeit — diese Card erscheint auf praktisch jedem Screen), `ChatScreen.kt`s kompletter Angebots-Status (ausstehend/akzeptiert/abgelehnt/storniert + Akzeptieren/Ablehnen/Stornieren-Buttons + Reserviert-/Blockiert-Banner + Signalement-Bestätigung — vorher der dichteste Emoji-Cluster der App, jetzt `Lock`/`Block`/`HourglassEmpty`/`CheckCircle`/`Cancel`/`Check`/`Close`, neue lokale `StatusRow`-Hilfs-Composable für die drei Offer-Status-Badges), `ChatListScreen.kt`s Blockiert-Indikator neben dem Konversationsnamen, `ListingDetailScreen.kt`s Login-Button/Kontakt-Karte (Telefon/WhatsApp/Chat-Button, alle drei jetzt mit `Icons.Filled.Phone`/`ChatBubbleOutline`, letzteres identisch zu `HomeScreen`s Nachrichten-Icon), Preis-Trend-Indikator (📉/📈→`Icons.AutoMirrored.Filled.TrendingDown/Up`), Bewertung-abgeben-Button (⭐→`Star`) und Signalement-Button/-Bestätigung (🚩→`Flag`, ✅→`CheckCircle`), `ListingsScreen.kt`s Suche-speichern-Bestätigung+Button (✅→`CheckCircle`, 🔔→`Bookmark`), `HomeScreen.kt`s "Vos centres d'intérêt"-Kachel-Fallback für unbekannte Kategorien (🏷️→wiederverwendet jetzt einfach `CategoryIcon("OTHER", ...)` statt eines Sonder-Icons), sowie das wiederkehrende "📌 Anzeigentitel als Link"-Muster in `AdminScreen.kt`/`NotificationsScreen.kt`/`SellerProfileScreen.kt` (→ neues `Icons.Filled.Link`, konsistent an allen drei Stellen). **Erfolgreich gebaut** (`assembleDebug` BUILD SUCCESSFUL, keine neuen Warnungen — `TrendingDown`/`TrendingUp` bewusst gleich als `AutoMirrored`-Variante eingebaut, um die vom Compiler direkt vorgeschlagene Deprecation-Warnung zu vermeiden). **Interaktive Emulator-Verifikation durch schwere Sandbox-Last verhindert**: zwei aufeinanderfolgende system-weite ANRs bis hin zum `system_server`-Prozess selbst (nicht nur die App oder der Launcher) — deutlich schwerwiegender als die in Tranche 9-13 dokumentierten Fälle, ein Neustart hätte hier vermutlich den vollen Emulator-Neustart gebraucht. Die App selbst rendert dabei sichtbar korrekt (Home-Screen mit allen Kategorie-Icons lud im Hintergrund der ANR-Dialoge fehlerfrei), `grep FATAL` im Logcat bestätigt keine App-Abstürze — der Code gilt als kompilier- und sichtbar-korrekt, aber ein vollständiges Durchklicken aller geänderten Screens (Chat-Angebotsfluss, Anzeigendetail-Kontaktkarte) steht noch aus.

- **Tranche 17 (2026-09-23): Icon-Professionalisierung Teil 2 — auch die in Tranche 14 bewusst ausgenommenen Empty-States** — zieht Webs heutigen Nachfolge-Commit nach (`c3da05e`, ausgelöst durch einen Nutzer-Screenshot eines übergroßen Kategorie-Emoji-Platzhalters + der expliziten Anweisung, wirklich *alle* Emoji zu prüfen, nicht nur das gezeigte). Web hat dabei seine eigene Tranche-14-Ausnahme für große dekorative 40sp-Empty-State-Icons revidiert; Android zieht das 1:1 nach, damit beide Plattformen wieder deckungsgleich sind. Ersetzt: `MesAnnoncesScreen.kt` (Leer-Zustand `📋`→`Icons.AutoMirrored.Filled.Assignment`, Listenzeile `👁 … · 🕐 …`→separate `Icon(Visibility)`/`Icon(Schedule)` in einer `Row` statt eines einzelnen interpolierten Strings — ein Vektor-Icon lässt sich nicht in einen `Text()`-String einbetten, exakt Webs eigener `icon-label`-Beweggrund —, Ablaufhinweis `⏳ …`→`Icon(HourglassEmpty)`), `ChatListScreen.kt` (`💬`→`ChatBubbleOutline`), `DeposerAnnonceScreen.kt` (Login-Gate `🔐`→`Lock`, Erfolgsseite `✅`→`CheckCircle`), `FavorisScreen.kt` (`🤍`→`FavoriteBorder`, semantisch passender als das bisherige gefüllte Herz für einen *leeren* Zustand), `NotificationsScreen.kt`/`SavedSearchesScreen.kt` (`🔔`→`NotificationsNone`), `AdminScreen.kt` (Meldungen-Leerzustand `🚩`→`Flag`, Admin-Notiz-Präfix `📝`→`Icon(Edit)` an allen 3 Fundstellen), `ProfilScreen.kt` (ID-Verifizierung/Telefon-verifiziert-Häkchen `✓`→`CheckCircle`), `SellerProfileScreen.kt` (Antwortzeit-Pille `⚡`→`Bolt`), `ListingDetailScreen.kt` (Bewertung-abgegeben-Hinweis `✅`→`CheckCircle`, in Tranche 14 übersehen), `CommonComponents.kt` (`VerifiedPill`s `✓`→`CheckCircle`, `SuccessBanner`s `✅`→`CheckCircle`), `HomeScreen.kt` (👋-Winke-Emoji ersatzlos entfernt, an beiden Stellen — mirrort Webs Entscheidung, rein dekorative Emoji ohne Icon-Ersatz zu streichen statt zu ersetzen). Bewusst weiterhin unangetastet (Ausnahmen bleiben gültig, nur enger gefasst): `CategoryConfig.emoji`-Datenfeld, `countryFlag()`, `★`-Bewertungsglyphen, der `✓`/Zahl-Stepper im Anzeige-Assistenten (zu kleines, rein typografisches Glyph) und `MACHINE_TYPE_ICONS` — bei letzterem diesmal **nicht mehr aus technischer Notwendigkeit** (anders als Webs natives `<select><option>`, kann Jetpack Composes `DropdownMenu` durchaus beliebige Composables pro Eintrag rendern), sondern aus reinem Aufwand/Nutzen-Grund: 20 neue Vektor-Icons für ein einzelnes Auswahlfeld wären ein eigenständiges, größeres Vorhaben für einen separaten Anlauf. i18n aller 6 Sprachen erneut aus dem Web-Repo synchronisiert (24 entfernte Emoji-Präfixe u. a. in `admin.*`/`deposer.*`/`profil.*`-Keys). **`./gradlew assembleDebug` bestätigt BUILD SUCCESSFUL** (volle APK-Assemblierung), keine neuen Deprecation-Warnungen (Assignment bewusst als `AutoMirrored`-Variante importiert). **Kein Emulator-Test in dieser Runde** — rein Compile-/Build-verifiziert, analog zu Tranche 16; ein visuelles Durchklicken der geänderten Empty-States steht für eine künftige Session mit funktionierendem Emulator noch aus.

---

## Testen

Reihenfolge pro Phase:
1. `./gradlew assembleDebug` — muss sauber durchlaufen (`BUILD SUCCESSFUL`), keine neuen Fehler.
2. Backend-Dev-Server muss auf Port 3000 laufen (`soukmar-backend`, `npm run dev` o.ä.).
3. Emulator starten (AVD `soukmar_test`, falls vorhanden, sonst neu anlegen — niedrige Auflösung wählen, siehe oben) und die neue/geänderte Funktion tatsächlich durchklicken (adb screencap + adb input, siehe "Bekanntes Problem" oben für Geschwindigkeits-Workarounds).
4. Demo-Accounts: `ahmed@soukmar.ma` / `fatima@soukmar.ma` / `youssef@soukmar.ma`, Passwort jeweils `soukmar123`.
5. Erst nach erfolgreichem Test committen + zu `github.com/azizennajih/soukmar-android` pushen.

**Achtung Workflow-Datei:** `.github/workflows/android-build.yml` kann mit dem aktuellen GitHub-Token nicht gepusht werden (fehlender `workflow`-Scope) — bleibt lokal im Arbeitsverzeichnis, nicht versioniert. Nicht erneut versuchen zu pushen, bis der Nutzer den Token-Scope erweitert hat.

---

## Wichtige Dateipfade

| Was | Pfad |
|-----|------|
| API-Interface | `app/src/main/java/com/soukmar/app/data/remote/ApiService.kt` |
| DTOs | `app/src/main/java/com/soukmar/app/data/remote/dto/` |
| Repositories | `app/src/main/java/com/soukmar/app/data/repository/` |
| Token/Session | `app/src/main/java/com/soukmar/app/data/local/TokenManager.kt` |
| Farben/Theme | `app/src/main/java/com/soukmar/app/ui/theme/` |
| Katalog-Modelle (Kategorien etc.) | `app/src/main/java/com/soukmar/app/ui/model/CatalogModels.kt` |
| Wiederverwendbare Composables | `app/src/main/java/com/soukmar/app/ui/components/` |
| Screens | `app/src/main/java/com/soukmar/app/ui/screens/<feature>/` |
| Navigation | `app/src/main/java/com/soukmar/app/ui/navigation/` |
